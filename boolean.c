#include <stdio.h>
// * BOOLEAN TYPE IS NOT BUILD IN TYPE IN C, SO WE NEED TO IMPORT IT
#include <stdbool.h>

bool ISADMIN = true;
bool ISSTUDENT = false;

int main()
{
    printf("%d\n", 8 > 9); // ~ 1 = true, 0 = false; retru nmay be 0 or 1; 
    return 0;
}